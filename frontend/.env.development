# Development environment.
# Use 127.0.0.1 (not "localhost") so the browser always hits the backend over
# IPv4 — the backend binds 127.0.0.1 only, and "localhost" resolves to IPv6 (::1)
# first on Windows, which causes intermittent "Failed to fetch".
VITE_API_URL=http://127.0.0.1:3000
